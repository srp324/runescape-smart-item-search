# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/game_items

# Embedding Model
# Options: all-MiniLM-L6-v2 (384 dims, fast), all-mpnet-base-v2 (768 dims, better quality)
EMBEDDING_MODEL=all-MiniLM-L6-v2

# OpenAI Configuration (if using OpenAI embeddings)
# OPENAI_API_KEY=your-api-key-here

# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS (comma-separated origins for production)
# Example: CORS_ORIGINS=https://your-web-domain.com,https://your-mobile-app.com
# Leave empty to use default development origins
CORS_ORIGINS=

