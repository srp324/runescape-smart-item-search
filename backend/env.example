# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/game_items

# Embedding Model
# Options: 
#   - Qwen/Qwen3-Embedding-0.6B (1024 dims, state-of-the-art quality, default)
#   - all-MiniLM-L6-v2 (384 dims, fast)
#   - all-mpnet-base-v2 (768 dims, better quality)
#   - Qwen/Qwen3-Embedding-4B (2560 dims)
#   - Qwen/Qwen3-Embedding-8B (4096 dims)
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-0.6B

# Embedding Dimension (optional)
# If set, overrides automatic dimension detection from EMBEDDING_MODEL
# Supported: 384, 768, 1024, 2560, 4096
# EMBEDDING_DIMENSION=1024

# OpenAI Configuration (if using OpenAI embeddings)
# OPENAI_API_KEY=your-api-key-here

# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS (comma-separated origins for production)
# Example: CORS_ORIGINS=https://your-web-domain.com
# Leave empty to use default development origins
CORS_ORIGINS=

