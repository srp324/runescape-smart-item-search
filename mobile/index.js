/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';

// Register the app component
// Replace 'RuneScapeItemSearch' with your actual app name if different
AppRegistry.registerComponent('RuneScapeItemSearch', () => App);

